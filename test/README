
initialize-source-sm-client:            Playback + EcalDQM-Source + StorageManager + EcalDQM-Client

initialize-source-sm-standalone:        Playback + StorageManager + EcalDQM-Source&Client

initialize-source-sm-standalone-daemon: Playback + StorageManager + EcalDQM-Source&Client + (external Collector)

--- terminal1:

cd CMSSW_1_4_0/
eval `scramv1 run -csh`

./bin/slc4_ia32_gcc345/DQMCollector

--- terminal2:

cd CMSSW_1_4_0/src/DQM/EcalBarrelMonitorClient/test/
eval `scramv1 run -csh`

./initialize-source-sm-standalone -host=localhost.localdomain -collector=localhost.localdomain

(after log/localhost.localdomain.40000.log & log/localhost.localdomain.40001.log are 'Ready')
./configure

(after log/localhost.localdomain.40000.log & log/localhost.localdomain.40001.log are 'Ready')
./enable

(if needed)
./stop
./halt

(cleanup)
./destroy

--- terminal3:

cd CMSSW_1_4_0/src/DQM/EcalBarrelMonitorClient/test/

tail -f log/localhost.localdomain.40000.log

--- terminal4:

cd CMSSW_1_4_0/src/DQM/EcalBarrelMonitorClient/test/

tail -f log/localhost.localdomain.40001.log

--- terminal5:

htmlview http://localhost.localdomain:40000

htmlview http://localhost.localdomain:40001

